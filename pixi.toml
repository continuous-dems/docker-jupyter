[workspace]
authors = ["Matt Fisher <mfisher87@gmail.com>"]
channels = ["conda-forge", "nodefaults"]
name = "cudem-docker-image"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[pypi-dependencies]
# Continuous DEMs
# NOTE: pinned to exact version so we control it manually
# IMPORTANT: Currently getting it from PyPI because it's not on Conda Forge yet
#            (https://github.com/continuous-dems/globato/issues/114)
globato = "==0.2.12"


[dependencies]
# IMPORTANT: Currently locking to 3.13 because globato isn't on Conda Forge yet.
#            Since we get it from PyPI, we have to use its dependencies from PyPI too.
#            Fiona doesn't build wheels for Python 3.14.
python = "3.13.*"

# Jupyter
jupyterhub-singleuser = "*"
jupyterlab = ">=4.4.2"
jupyterlab-favorites = "*"
jupyterlab-geojson = "*"
jupyterlab-git = "*"
jupyterlab-myst = "*"
mystmd = "*"
nbdime = "*"

# Util
gh = "*"
gh-scoped-creds = "*"
git = "*"

# Data
gdal = "*"
geopandas = "*"
geos = "*"
h5netcdf = "*"
h5py = "*"
numpy = "*"
pandas = "*"
pystac-client = "*"
rasterio = "*"
rioxarray = "*"
scipy = "*"
shapely = "*"
xarray = "*"

# Viz
folium = "*"
hvplot = "*"
ipympl = "*"
leafmap = "*"
matplotlib = "*"

# Other
jupytergis = ">=0.15.0"
pre-commit = "*"
ruff = "*"
typst = "*"
urllib3 = "~=1.26.19"
